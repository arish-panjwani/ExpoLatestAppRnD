{
  "name": "expolatestapprnd",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "npmInstall": "npm cache clean -f && npm i --legacy-peer-deps",
    "npmInstallWeb": "npm cache clean -f && npm i --legacy-peer-deps && yarn web",
    "deleteNodeModules": "rm -rf node_modules && rm -rf package-lock.json",
    "watchManCacheClean": "watchman watch-del-all",
    "deleteAndInstall": "yarn deleteNodeModules && yarn watchManCacheClean && yarn npmInstall",
    "deleteAndInstallWeb": "yarn deleteNodeModules && yarn watchManCacheClean && yarn npmInstallWeb"
  },
  "dependencies": {
    "@expo/cli": "0.7.2",
    "@expo/webpack-config": "18.1.0",
    "expo": "48.0.18",
    "expo-camera": "13.2.1",
    "react": "18.2.0",
    "react-dom": "^18.2.0",
    "react-native": "0.71.11",
    "react-native-web": "0.18.12"
  },
  "devDependencies": {
    "@babel/core": "^7.22.5"
  },
  "private": true
}
